<!DOCTYPE html>
<html>
    <head>
        <% include ../static/partials/head.ejs %>
    </head>
    <body>
        <% include ../static/partials/navbar.ejs %>

        <main class="container">
        <% include ../static/partials/messages.ejs %>
            <h1>New Wiki</h1>

            <form action="/wikis/create" method="post">
                <div class="form-group">
                    <label for="title">Wiki title</label>
                    <input type="text" class="form-control" name="title" aria-describedby="titleHelp" placeholder="Enter Title">
                </div>
                <div class="form-group">
                    <label for="body">Body</label>
                    <small class="text-muted" id="bodyHelp">You may use markdown syntax</small>
                    <textarea name="body" rows="4" class="form-control" placeholder="Enter Body"></textarea>
                </div>
                <% if(currentUser.role === 1 || currentUser.role === 2){ %>
                    <div class="form-group">
                        <label for="private">Set Private?</label>
                        <input type="radio" name="private" value="true" checked>Yes
                        <input type="radio" name="private" value="false">No
                    </div>
                <% } %>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </main>

        <% include ../static/partials/baseScripts.ejs %>

    </body>
</html>